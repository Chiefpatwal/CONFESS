{
  "name": "confess-app",
  "version": "1.0.0",
  "private": true,
  "description": "A full-stack confession app with React frontend and Express backend",
  "scripts": {
    "install": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix backend",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run start --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run start --prefix frontend",
    "clean": "rm -rf frontend/build && rm -rf backend/dist",
    "postbuild": "echo 'Build completed. Frontend build should be in frontend/build/'",
    "prebuild": "echo 'Starting build process...'",
    "prestart": "echo 'Starting server...'"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "keywords": ["confession", "react", "express", "mongodb", "clerk"],
  "author": "Your Name",
  "license": "MIT"
}